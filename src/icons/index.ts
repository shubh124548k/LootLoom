/**
 * LootLoom — Icon catalogue.
 * LootLoom uses lucide-react as its icon library (uniform sizing, stroke, hover).
 * Import icons via: `import { IconName } from "lucide-react"`.
 *
 * This barrel re-exports the IconBadge component for consistent icon containers.
 */
export { IconBadge } from "@/components/lootloom/icon-badge";
