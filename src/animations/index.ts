/**
 * LootLoom — Animation catalogue (re-export).
 * Central entry point for all animation presets.
 * Actual presets live in src/lib/animations.ts (framer-motion variants).
 */
export * from "@/lib/animations";
